#include <iostream>
//for main we can just do .insert instead of reading in user input (if we are running low on time)
//since that still follows the skeleton code for assignment. If you have time to impliment user input
//that works too
int main() {
  // Code here...
  return 0;
}
